package ${basePackage}.action;

import java.io.Serializable;
import ${package}.model.${name};

public class ${name}$Act implements Serializable {

    public ${name}$Act(${name} self) {
        this.self = self;
    }

    private final ${name} self;
}
